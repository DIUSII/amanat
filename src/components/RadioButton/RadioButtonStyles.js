import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 24,
    width: 24,
    borderRadius: 12,
    borderWidth: 2,
    borderColor: '#392D92',
    alignItems: 'center',
    justifyContent: 'center',
  },
  check: {
    height: 12,
    width: 12,
    borderRadius: 6,
    backgroundColor: '#392D92',
  },
});
