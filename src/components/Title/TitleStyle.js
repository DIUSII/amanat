import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  titleContainer: {
    marginBottom: 20,
  },
  titleText: {
    fontWeight: '500',
    fontSize: 25,
    lineHeight: 37,
    textAlign: 'center',
    color: '#392D92',
  },

});

export default styles;
